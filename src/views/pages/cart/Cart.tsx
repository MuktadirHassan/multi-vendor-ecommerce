// import { AppState } from "redux/store";

const Cart = () => {
  // const products = useSelector((state: AppState) => state.cart);
  return (
    <div>
      {/* {products.map((item) => (
        <div>{item.name}</div>
      ))} */}
    </div>
  );
};

export default Cart;
